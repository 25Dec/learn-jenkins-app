pipeline {
    agent any

    stages {
        stage("Build App") {
            steps {
                sh '''
                    echo "Built App"
                '''
            }
        }
        stage("Test App") {
            steps {
                sh '''
                    echo "Tested App"
                '''
            }
        }
    }
}